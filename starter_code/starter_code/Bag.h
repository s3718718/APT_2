#include <cstdlib>
#include "Tile.h"

class Bag {
    public:
        Bag();
        int size();
        Tile pullTile();
        void print();

    private:
        // Make ordered linked lists of tiles
};